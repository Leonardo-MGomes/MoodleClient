[project]
name = "moodleclient"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "pydantic[email]>=2.13.4",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
